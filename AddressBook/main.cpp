#include<math.h>
#include "funs.h"

AddressBook adds[LEN];
//需要一个哈希表 、name-index节省查询时间
unordered_map<string,UINT> names;
UINT addc = 1;
int main() {
    show(&addc);
    exit(0);
    system("pause");//冻结屏幕

}
